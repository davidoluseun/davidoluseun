type projectTypes = {
  _id: string;
  name: string;
  repo: string;
  link: string;
  desc: string;
  stack: string[];
};

type MessageTypes = {
  name: string;
  email: string;
  subject?: string;
  phone?: string;
  message: string;
};
